export const ROUTES = {
  LOGIN: 'Login',
  REGISTER: 'Register',
  CHAT_LIST: 'ChatList',
  DRAWER: 'Drawer',
  CHAT_ROOM: 'ChatRoom',
  AUTH: 'Auth',
} as const;

// export type RouteNames = typeof ROUTES[keyof typeof ROUTES];
